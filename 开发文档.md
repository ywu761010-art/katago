# katago 开发文档

目标：做一个能自己下棋、自己对弈、自己训练的围棋程序。

语言分工已定：

```
Java
 ├── 围棋规则
 ├── MCTS 搜索
 └── 游戏引擎（对局、落子、终局）

Python
 ├── PyTorch
 ├── 神经网络训练
 └── 模型导出
```

仓库：`https://github.com/ywu761010-art/katago.git`  
本地：`D:\code\katago`  
主分支：`main`

本文是开发真源。目录、接口、阶段有变化时，先改本文再改代码。

---

## 1. 要做成什么样

最终形态：

1. Java 引擎能按规则下棋（合法点、提子、劫、停着、胜负）。
2. Java 用 MCTS 选点；搜索时询问神经网络要「各点落子概率」和「局面价值」。
3. Python 用对局数据训练网络，把模型交给 Java 使用。
4. 闭环：Java 自对弈 → 写出棋谱 → Python 训练 → 新模型再回到 Java。

第一版不追求 19 路职业强度。先在 **9 路** 上跑通闭环，再扩到 13 / 19 路。

---

## 2. 为什么拆成 Java + Python

| 部分 | 语言 | 原因 |
| --- | --- | --- |
| 规则、棋盘、对局循环 | Java | 状态机、对象模型清楚，调试方便 |
| MCTS | Java | 搜索是大量循环和树操作，放引擎里，不进 Python |
| 训练 | Python + PyTorch | 生态成熟，改网络快 |
| 对局时的推理 | Java 加载导出后的模型 | 搜索每步要问网络很多次，不能每步都启动一次 Python 训练脚本 |

训练和搜索不混在一个进程里。Python 负责「学会下棋」，Java 负责「当场下棋」。

---

## 3. 模块怎么接

```
        ┌─────────────────────────────────────┐
        │              Java 引擎               │
        │  Board / Rules → Game → MCTS        │
        │                      │              │
        │                      ▼              │
        │              推理接口（ONNX）         │
        └──────────────────────┬──────────────┘
                               │ 输入：当前局面
                               │ 输出：policy + value
                               ▼
                        模型文件 model.onnx
                               ▲
                               │ 导出
        ┌──────────────────────┴──────────────┐
        │           Python 训练端              │
        │  棋谱数据 → Dataset → PyTorch 训练    │
        │                 → checkpoint.pt      │
        └─────────────────────────────────────┘

自对弈：Java 打完一盘 → 写成 JSON → Python 读入再训练
```

### 3.1 棋盘约定

- 坐标：`x` 从左到右 `0 .. size-1`，`y` 从下到上 `0 .. size-1`（与 GTP 的 A1 在左下角一致）。
- 点的一维编号：`y * size + x`。停着（pass）单独用编号 `size * size`。
- 颜色：黑 `1`，白 `-1`，空 `0`。轮到谁下用 `toPlay` 表示。
- 第一版规则：中国数子、贴 7.5 目（9 路可先用 7.5，以后按棋盘再调）、简单劫（禁止立刻回提），允许停着。 Superko 第一版不做。

### 3.2 网络输入 / 输出

第一版特征尽量少，够 MCTS 用即可：

- 输入张量形状：`[1, C, size, size]`
- 通道（C = 3）：
  1. 己方棋子
  2. 对方棋子
  3. 当前轮到己方（整层填 1）
- 输出：
  - `policy`：长度 `size*size + 1` 的 logits（含 pass）
  - `value`：标量，从当前行棋方视角，约在 `[-1, 1]`（负负、正胜、接近 0 接近和）

Java 和 Python 必须按同一套特征编码。编码逻辑写两份时，用同一组测试棋面核对。

### 3.3 对局数据格式

自对弈一盘写成一个 JSON，例如 `data/selfplay/20260912_001.json`：

```json
{
  "size": 9,
  "komi": 7.5,
  "result": 1,
  "moves": [
    {"color": 1, "pos": 40, "policy": [0.01, 0.02], "value": 0.12}
  ]
}
```

- `result`：黑胜 `1`，白胜 `-1`，和 `0`（贴目后数子，和棋极少）。
- `pos`：落子一维编号，pass 为 `size*size`。
- `policy` / `value`：当时 MCTS 统计，供训练用。没有网络的阶段可以先只存落子和胜负。

### 3.4 推理接口（Java）

Java 侧只依赖一个接口，方便先假实现、后换真模型：

```text
infer(board, toPlay) → { policy: float[size*size+1], value: float }
```

实现顺序：

1. `RandomInfer`：均匀策略，value = 0。用来测 MCTS 和规则。
2. `OnnxInfer`：加载 Python 导出的 `model.onnx`。

不采用「Java 每步调 Python 脚本」。早期若要联调，可以临时用本机 HTTP，但正式路径是 ONNX。

---

## 4. 目标目录

代码还没铺，按这个结构建：

```
katago/
├── README.md
├── 开发文档.md          ← 本文
├── LICENSE
├── .gitignore
│
├── engine/              ← Java：规则、引擎、MCTS、推理
│   ├── pom.xml
│   └── src/
│       ├── main/java/katago/
│       │   ├── board/      棋盘、落子、提子、劫
│       │   ├── game/       对局、胜负、自对弈写出 JSON
│       │   ├── mcts/       搜索树、PUCT
│       │   ├── infer/      Infer 接口、RandomInfer、OnnxInfer
│       │   └── Main.java   命令行入口
│       └── test/java/katago/
│
├── train/               ← Python：数据、网络、训练、导出
│   ├── requirements.txt
│   ├── katago_train/
│   │   ├── encode.py     与 Java 相同的特征编码
│   │   ├── dataset.py
│   │   ├── model.py
│   │   ├── train.py
│   │   └── export_onnx.py
│   └── tests/
│
├── models/              ← 导出的权重（大文件默认不进 Git）
│   └── *.onnx
│
└── data/                ← 自对弈棋谱（默认不进 Git）
    └── selfplay/
```

- Java 构建：Maven，JDK 17。
- Python：3.11+，PyTorch，onnx。
- `models/`、`data/` 只放本地产物；仓库里最多放一个很小的示例 json，方便测试。

---

## 5. 技术选型

| 项 | 选择 | 备注 |
| --- | --- | --- |
| JDK | 17 | LTS，本机先确认 `java -version` |
| Java 构建 | Maven | `engine/pom.xml` |
| MCTS | PUCT | 先验用 policy，Q 用 value + 对局回传 |
| 推理 | ONNX Runtime Java | 训练端 `torch.onnx.export` |
| Python | 3.11+ | venv 放 `train/.venv/`，不提交 |
| 深度学习 | PyTorch | 第一版小网络：几层卷积即可 |
| 棋盘大小 | 先 9，后 13/19 | 规则代码按 `size` 参数写，不要写死 19 |
| 对弈协议 | 先自己的 CLI，再 GTP | GTP 方便以后接 Sabaki 等棋谱软件 |

第一版网络规模（9 路）：

- 残差或普通卷积 4～8 层，通道 32～64
- 能在普通 CPU 上推理；有 GPU 再开 CUDA 训练

---

## 6. 开发阶段

每阶段结束时必须能运行，再进入下一阶段。不要把规则、MCTS、训练一次写完。

### 阶段 0 — 仓库与文档（当前）

- [x] Git 仓库可用
- [x] 本文确定方案 B（Java 引擎 + Python 训练）
- [ ] 按第 4 节建空目录和 `.gitignore`（Python / Maven / data）

### 阶段 1 — Java 围棋规则

可运行标准：命令行下一盘人人对战（或脚本双方随机落子）不崩溃，提子、自杀点、简单劫正确。

- 棋盘、落子、气、提子、禁自杀、简单劫、pass
- 终局数子 + 贴目
- 单测：边角提子、劫、自杀、双活暂不要求完美（第一版终局可双方连 pass 后按盘面数子）

### 阶段 2 — Java 游戏引擎

可运行标准：随机机器人 vs 随机机器人能打完并写出 JSON。

- 对局循环、轮流、认输/停着结束
- 把棋谱写到 `data/selfplay/`

### 阶段 3 — Java MCTS（无网络）

可运行标准：MCTS 随机先验 对 纯随机，MCTS 胜率明显高于随机。

- 树节点、选择（PUCT）、扩展、回传
- 接 `RandomInfer`
- 参数先写死：模拟次数、`c_puct`，以后再做成配置

### 阶段 4 — Python 网络与训练

可运行标准：用阶段 2/3 的 JSON 能 loss 下降，并导出 `models/dev.onnx`。

- 特征编码与 Java 对齐
- 小 CNN：policy + value 双头
- `train.py`、`export_onnx.py`

### 阶段 5 — 接上真模型

可运行标准：Java MCTS 加载 `dev.onnx` 下一盘，比无网络 MCTS 更像「会选大场」。

- `OnnxInfer`
- 编码单测：同一棋面 Java / Python 输入张量一致

### 阶段 6 — 自对弈闭环

可运行标准：跑 N 盘自对弈 → 训练一轮 → 换模型 → 再打，流程用脚本串起来。

之后才考虑：19 路、更强网络、超级劫、GTP、GUI。

---

## 7. 环境

| 工具 | 用途 |
| --- | --- |
| Git | 版本管理 |
| JDK 17+ | 编译运行 `engine` |
| Maven | Java 构建 |
| Python 3.11+ | 训练 |
| Cursor / IDEA | 编辑；Java 用 IDEA 或 Cursor 均可 |

```bash
java -version
mvn -version
python --version
```

Python 依赖（阶段 4 再装，写入 `train/requirements.txt`）：

```
torch
numpy
onnx
```

有 NVIDIA GPU 时安装对应的 CUDA 版 PyTorch；没有 GPU 就用 CPU，9 路足够。

---

## 8. Git 工作流

```bash
cd D:\code\katago
git pull origin main
# ... 改代码，跑通当前阶段的验收 ...
git status
git add .
git commit -m "说明这次完成了哪个阶段的哪一块"
git push origin main
```

`git push` 时 GitHub 不接受登录密码，用 PAT 或 SSH。本机若还没有 `~/.ssh`，需要推送时再配。

不要提交：`.class`、`target/`、`.venv/`、`__pycache__/`、`.onnx` 大模型、自对弈大数据。

国内访问 GitHub 可能偶发重置或超时，过一会儿重试 `git pull` / `git push`。`ping github.com` 不通是正常的。

---

## 9. 约定

- Java 包名：`katago.*`。Python 包名：`katago_train`。
- 棋盘大小、贴目、模拟次数不要在逻辑里写死多个副本，集中放配置。
- 先正确后加速。规则错了再快的 MCTS 也没有意义。
- 改特征或 JSON 字段时，Java、Python、本文三处一起改。
- 提交说明写原因，例如 `实现 9 路提子与简单劫`，不要写 `update`。

---

## 10. 当前进度

| 阶段 | 状态 |
| --- | --- |
| 0 方案与文档 | 进行中 |
| 1 Java 规则 | 未开始 |
| 2 游戏引擎 | 未开始 |
| 3 MCTS | 未开始 |
| 4 Python 训练 | 未开始 |
| 5 ONNX 接入 | 未开始 |
| 6 自对弈闭环 | 未开始 |

下一步（文档之后的第一段代码）：建 `engine/`、`train/` 目录，把 `.gitignore` 补全，实现阶段 1 的棋盘与规则。
