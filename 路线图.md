# katago 路线图

本文管「做到哪、先做啥、做到什么算完」。  
怎么接模块、目录和命令见 [开发文档.md](开发文档.md)。

两份文档有出入时，**接口和目录以开发文档为准，阶段顺序以本路线图为准**。

---

## 北极星

9 路上跑通闭环：

**Java 按规则下棋 → MCTS 选点 → 神经网络给建议 → 自对弈出棋谱 → Python 训练 → 新模型再回去下。**

第一版不追求打败职业棋手，也不先做 19 路。闭环能转，再加棋盘和强度。

---

## 三条线

```
规则引擎 ──► 对局引擎 ──► MCTS ──► 接模型 ──► 自对弈闭环
                                      ▲
棋谱 JSON ──► PyTorch 训练 ──► ONNX ──┘
```

| 线 | 负责 | 语言 |
| --- | --- | --- |
| 棋 | 规则、落子、终局、对局循环 | Java |
| 搜 | MCTS（PUCT） | Java |
| 学 | 训练、导出模型 | Python |

三条线最后在「MCTS 问网络」处汇合。没有规则，搜索没有意义；没有搜索，训练没有高质量棋谱；没有训练，搜索只能乱走。

---

## 里程碑

每座里程碑必须能运行。做完再进下一座，不一次铺开。

### M0  立住项目（当前）

**目标**：方案和文档齐，仓库能继续往下写代码。

| 交付 | 验收 |
| --- | --- |
| 方案 B：Java 引擎 + Python 训练 | 开发文档写明职责和接口 |
| 本路线图 | 阶段顺序和验收标准清楚 |
| Git 仓库 | 本地 `D:\code\katago` 能对齐 GitHub |

**本座不做**：源码、训练脚本、模型。

**状态**：进行中（文档已有，目录和 `.gitignore` 还没按开发文档建完）。

---

### M1  棋盘会走规则

**目标**：9 路能按围棋规则落子，不会走出非法棋。

| 交付 | 验收 |
| --- | --- |
| 棋盘、气、提子、禁自杀、简单劫、停着 | 人人对战或随机对下不崩溃 |
| 终局数子 + 贴 7.5 目 | 双方连 pass 后能报谁赢 |
| 规则单测 | 边角提子、劫、自杀点测过 |

**本座不做**：AI、训练、19 路、超级劫、双活的完整形势判断。

---

### M2  能打完一盘并记下

**目标**：两台随机机器人能下完整盘，棋谱落到磁盘。

| 交付 | 验收 |
| --- | --- |
| 对局循环（轮流、pass、结束） | 随机 vs 随机能打完 |
| JSON 棋谱写入 `data/selfplay/` | 文件能被人读懂，字段符合开发文档 |

**本座不做**：MCTS、神经网络。

---

### M3  无网络的 MCTS 能赢随机

**目标**：搜索本身是有效的，不依赖模型。

| 交付 | 验收 |
| --- | --- |
| PUCT、树的选择 / 扩展 / 回传 | 能下一盘 |
| `RandomInfer`（均匀策略，value = 0） | MCTS vs 纯随机，MCTS 胜率明显更高 |

**本座不做**：PyTorch、ONNX。参数可先写死。

---

### M4  网络能学会棋谱

**目标**：Python 端能吃棋谱、掉 loss、导出模型。

| 交付 | 验收 |
| --- | --- |
| 与 Java 相同的特征编码 | 同一棋面两边张量一致（可在 M5 再硬核核对） |
| 小 CNN：policy + value | 在 M2/M3 的 JSON 上 loss 下降 |
| 导出 `models/dev.onnx` | 文件能生成 |

**本座不做**：Java 加载模型、19 路大网。

---

### M5  引擎真正用上模型

**目标**：搜索问的是训练出来的网，不再是随机先验。

| 交付 | 验收 |
| --- | --- |
| `OnnxInfer` | Java 加载 `dev.onnx` 能下一盘 |
| 编码对齐单测 | 同一局面 Java / Python 输入一致 |
| 观感对比 | 比无网络 MCTS 更会抢大场、少乱填 |

**本座不做**：自动循环训练、GUI。

---

### M6  闭环能转（第一版完成）

**目标**：一条命令（或短脚本）能：自对弈 → 训练 → 换模型 → 再下。

| 交付 | 验收 |
| --- | --- |
| 自对弈批量出谱 | 能指定盘数 |
| 训练脚本吃新谱 | 能产出新的 onnx |
| 引擎换模型再打 | 流程可重复跑，不靠手工拷文件猜路径 |

**到这里，第一版结束。** 9 路闭环成立，再考虑加棋盘和强度。

---

## 第一版之后（不排进当前开发）

这些事只有 M6 跑通才启动，避免提前分心：

| 方向 | 内容 |
| --- | --- |
| 更大的棋盘 | 13 路，再 19 路（规则按 `size` 写，不要写死 9） |
| 更强的网络 | 残差加宽加深、GPU 训练 |
| 更完整的规则 | 超级劫、更好的终局 / 形势 |
| 对外接口 | GTP，接到 Sabaki 等棋谱软件 |
| 界面 | 简易 GUI 或网页看棋 |
| 强度验证 | 固定对手、Elo、回归对局 |

---

## 现在在哪

```
M0 文档  ●━○  M1 规则  ○  M2 引擎  ○  M3 MCTS  ○  M4 训练  ○  M5 接模型  ○  M6 闭环
         ▲
       你在这里
```

| 里程碑 | 状态 |
| --- | --- |
| M0 立住项目 | 进行中 |
| M1 规则 | 未开始 |
| M2 引擎 | 未开始 |
| M3 MCTS | 未开始 |
| M4 训练 | 未开始 |
| M5 接模型 | 未开始 |
| M6 闭环 | 未开始 |

下一步：收尾 M0（建 `engine/`、`train/`、补 `.gitignore`），开工 M1 棋盘与规则。
