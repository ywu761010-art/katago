# katago 任务清单

总目标：9 路跑通「规则 → MCTS → 训练 → 再下棋」。第一版不含 19 路、GTP、GUI。

阶段顺序以 [路线图.md](路线图.md) 为准。接口以 [开发文档.md](开发文档.md) 为准。

图例：`[x]` 已完成 · `[~]` 进行中 · `[ ]` 未开始

---

## 两人怎么分

| 角色 | 负责 | 只改这些目录 |
| --- | --- | --- |
| **甲 · 引擎（你）** | Java 规则、对局、MCTS、加载模型 | `engine/` |
| **乙 · 训练** | Python 特征、网络、训练、导出 onnx | `train/` |

甲只在 `jia/*` 分支改 `engine/`，乙只在 `yi/*` 分支改 `train/`。命名规则见开发文档第 8.1 节。  
公共约定（JSON 字段、特征通道）先改开发文档，甲乙再一起改代码，不要各改各的。

乙不必等 MCTS。开发文档里的 JSON / 特征已经够乙用假棋谱开工。甲是关键路径：规则没对，后面全是空转。

---

## 大概多久

按「第一版闭环」估，不是 19 路强度。

| 投入 | 日历 | 说明 |
| --- | --- | --- |
| 每天各约 3 小时 | **约 3 周** | 周末各留一天验收 |
| 业余每晚 1–2 小时 | **约 6–8 周** | 见下面 6 周表 |
| 规则或 MCTS 卡壳 | **再加 1–2 周** | 这两块最容易超期 |

合计大约 **20 人天**（甲约 12，乙约 8）。两个人不能把 20 天变成 10 天，因为 M1→M2→M3 必须串行。乙并行只能把总日历从「一个人 4–5 周认真干」压到「两人 3 周认真干」。

### 业余 6 周日历

| 周 | 甲 · Java | 乙 · Python | 对齐点 |
| --- | --- | --- | --- |
| 1 | M0 收尾，开工 M1 | 冻结约定，搭 Python 环境 | GitHub 能 push；目录定死 |
| 2 | M1 提子 / 劫 / 数子 / 单测 | 特征编码 + 小 CNN | 测试棋面的张量长什么样 |
| 3 | M2 随机对局写出 JSON | `train.py` 降 loss | 棋谱字段和文档一致 |
| 4 | M3 MCTS 赢随机 | 导出 `models/dev.onnx` | Infer 形状锁死 |
| 5 | Java 加载 onnx 下棋 | 编码对齐单测 | 同一局面两边输入一致 |
| 6 | 批量自对弈、换模型 | 新谱再训练 | 一条脚本转完一轮 |

### 认真投入压成 3 周

| 周 | 甲 | 乙 | 周末验收 |
| --- | --- | --- | --- |
| 1 | M0 + M1 | 编码 + 网络骨架 | 随机对下不崩溃 |
| 2 | M2 + M3 | 训练通、导出 onnx | MCTS 明显赢随机 |
| 3 | M5 + M6 引擎侧 | M5 + M6 训练侧 | 脚本能转一整轮 |

---

## 第一周任务（开工日起 7 天）

目标只三件：**目录能编译、约定不再改、甲能在 9 路上落下第一手。**  
不做：劫、数子、MCTS、真正训练。那些是第二周。

每人大约 8–10 小时。甲只动 `engine/`，乙只动 `train/` 和示例数据。

### 周末验收（第 7 天一起看）

- [ ] `engine` 能 `mvn compile`，命令行能打印 9 路棋盘并落下至少一手合法棋
- [ ] `train` 虚拟环境能起来；`encode.py` 对空棋盘输出形状 `[1, 3, 9, 9]`
- [ ] 仓库里有一份最小示例 JSON，字段和开发文档 3.3 节一致
- [ ] 现有文档已经 `commit`；能 push 就 push，网络不通就第二天再试

### 甲 · Java（每天约 1.5 小时）

| 天 | 任务 | 当天做完的样子 |
| --- | --- | --- |
| 1 | 建 `engine/`、`pom.xml`、空包 `katago.board` | `mvn compile` 通过 **[x]** |
| 2 | `Main` 打印空 9 路棋盘 | 终端能看到 9×9 |
| 3 | `Board`：坐标、一维编号、黑白空 | 给定 `x,y` 能读到颜色 |
| 4 | 落子（先允许任何空点，不管气） | 命令行能下一手并刷新棋盘 |
| 5–6 | 气、提子、禁自杀 | 边角一口气被提掉；自杀点下不进去 |
| 7 | 和乙对验收；简单劫留给第二周 | 上面「周末验收」甲那几条打勾 |

对应清单：M0-7、M0-9、M1-1、M1-2。M1-3 的劫、M1-4 数子本周不做。

### 乙 · Python（每天约 1.5 小时）

| 天 | 任务 | 当天做完的样子 |
| --- | --- | --- |
| 1 | 建 `train/`，用 uv 固定 Python 3.12 并装 torch | `uv run python -c "import torch"` 能打出版本 |
| 2 | 包 `katago_train`，空的 `encode.py` | 能 `from katago_train.encode import encode` |
| 3 | 按开发文档写一份 `data/selfplay/example.json` | 只有几手棋，字段齐全 |
| 4–5 | 实现 3 通道编码：己方 / 对方 / 轮次 | 空棋盘、轮到黑，打印 tensor 形状 |
| 6 | 测试：形状 `[1,3,9,9]`，policy 位长度 82 | `tests/` 里一条断言通过 |
| 7 | 和甲对验收；不写 `train.py` | 上面「周末验收」乙那几条打勾 |

对应清单：M0-8、M4-1、M4-2 的开头。`model.py` / `train.py` 第二周再写。

### 乙 · 执行明细（只动 `train/` 和示例 JSON）

本机目前只有 **Python 3.14**，torch 对 3.14 基本没轮子。第 1 天用 **uv** 安装并固定 3.12，不要用 3.14 硬装。不要改 `engine/`。本机若还没有 uv，先装：

```powershell
powershell -ExecutionPolicy Bypass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

装完重开终端，确认 `uv --version` 有输出。

**第 1 天（今天，约 1.5 小时）**

1. 建目录：
   - `train/katago_train/__init__.py`（空文件）
   - `train/tests/`
2. 在 `train/` 里初始化 uv 项目（Python 3.12）：

   ```powershell
   cd D:\code\katago\train
   uv init --name katago-train --python 3.12
   uv python install 3.12
   uv add torch numpy onnx
   uv run python -c "import torch; print(torch.__version__)"
   ```

   `uv init` 如果已经有目录，按提示保留现有文件即可。依赖写进 `pyproject.toml`，锁进 `uv.lock`，这两个要提交；`.venv` 不提交。

3. 收工标准：最后一行能打出 torch 版本。今晚不写编码。

**第 2 天**

- 写空函数 `train/katago_train/encode.py`：`encode(board, to_play, size=9)`，先 `raise NotImplementedError` 也行。
- 收工：在 `train/` 下 `uv run python -c "from katago_train.encode import encode"` 不报错。

**第 3 天**

- 写 `data/selfplay/example.json`，9 路、贴 7.5，至少 3 手。字段必须和开发文档 3.3 节一致：

  ```json
  {
    "size": 9,
    "komi": 7.5,
    "result": 1,
    "moves": [
      {"color": 1, "pos": 40, "policy": [0.01], "value": 0.12}
    ]
  }
  ```

- `pos`：`y * 9 + x`，pass 是 `81`。`policy` 第 3 天可以先写短数组，第 6 天再改成长度 82。

**第 4–5 天**

- 实现编码，通道顺序不能错：
  1. 己方棋子
  2. 对方棋子
  3. 当前轮到己方（整层填 1）
- 空棋盘、`to_play=1`（黑）时：通道 1、2 全 0，通道 3 全 1。
- 收工：打印 `tuple(tensor.shape)` 为 `(1, 3, 9, 9)`。

**第 6 天**

- `train/tests/test_encode.py` 至少两条断言：
  - 空棋盘形状 `(1, 3, 9, 9)`
  - policy 位长度 `9*9+1 = 82`（若 encode 只出输入，可另写 `policy_size(size) -> 82`）
- 收工：在 `train/` 下 `uv run pytest tests` 通过。

**第 7 天**

- 和甲对着周末验收清单跑。
- 不写 `train.py`、不训练、不导出 onnx。

**乙后面几周（先看，本周不要做）**

| 周 | 乙做什么 |
| --- | --- |
| 2 | 小 CNN（policy + value 双头） |
| 3 | `train.py`，用 example 或甲的棋谱把 loss 做下去 |
| 4 | `export_onnx.py` → `models/dev.onnx` |
| 5 | 和甲对同一棋面，两边输入张量一致 |
| 6 | 吃新自对弈棋谱，再导出一版 onnx |

### 两人一起（穿插，不要各改各的）

| 天 | 任务 | 谁 |
| --- | --- | --- |
| 1 | 定谁是甲、谁是乙；过一遍开发文档 3.1–3.3，本周不再改字段 | 共同 |
| 1 或 2 | GitHub 登录（Edge）+ 把现有文档 push 上去 | 有远程权限的人 |
| 1 | `.gitignore` 补上 `target/`、`.venv/`、`__pycache__/`、`models/`、`data/selfplay/`（示例 json 可提交） | 共同，提交一次就定 |
| 7 | 对着「周末验收」逐条跑，记下没过的，带进第二周 | 共同 |

第一周结束时允许：劫还没做、还不会训练、GitHub 如果又断了可以第二天补 push。不允许：还没有 `engine/` 和 `train/`，或编码形状和文档不一致。

---

## M0 立住项目 `[~]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M0-1 | 定方案 B：Java 规则/MCTS/引擎 + Python 训练/模型 | [x] | 共同 |
| M0-2 | 写开发文档（职责、接口、目录、约定） | [x] | 共同 |
| M0-3 | 写路线图（里程碑和验收） | [x] | 共同 |
| M0-4 | 写本任务清单（含两人排期） | [x] | 共同 |
| M0-5 | 本地仓库对齐 GitHub | [~] | 有远程权限的人登录后 push |
| M0-6 | 配 GitHub 推送认证（Edge / PAT / SSH） | [ ] | 有远程权限的人 |
| M0-7 | 建 `engine/`、`models/`、`data/selfplay/` | [x] | 甲 |
| M0-8 | 建 `train/`，补 `.gitignore` | [~] | gitignore 已补；`train/` 等乙 |
| M0-9 | 建 `engine/pom.xml`（JDK 17，先能编译空项目） | [x] | 甲 |

M0 收口：目录在、文档齐、能 `git push`。然后甲写棋盘，乙搭训练环境。

---

## M1 棋盘会走规则 `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M1-1 | 棋盘数据结构，`size` 可配，先 9 路 | [ ] | 甲 |
| M1-2 | 落子、气、提子 | [ ] | 甲 |
| M1-3 | 禁自杀、简单劫、停着（pass） | [ ] | 甲 |
| M1-4 | 终局数子 + 贴 7.5 目 | [ ] | 甲 |
| M1-5 | 合法点列表（给引擎和 MCTS 用） | [ ] | 甲 |
| M1-6 | 单测：边角提子、劫、自杀点 | [ ] | 甲 |
| M1-7 | 命令行人人对战或随机对下，能走完不崩溃 | [ ] | 甲写，两人各下一盘 |

M1 验收：非法棋下不出去；双方连 pass 能报谁赢。  
本座不做：AI、训练、19 路、超级劫。

---

## M2 能打完一盘并记下 `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M2-1 | 对局循环：轮流、pass、结束、认输 | [ ] | 甲 |
| M2-2 | 随机机器人（合法点里乱选） | [ ] | 甲 |
| M2-3 | 随机 vs 随机打完整盘 | [ ] | 甲 |
| M2-4 | 按开发文档把 JSON 写到 `data/selfplay/` | [ ] | 甲写，乙确认能读 |

M2 验收：能看到棋谱文件，人能读懂。  
本座不做：MCTS、神经网络。

---

## M3 无网络 MCTS `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M3-1 | `Infer` 接口：局面 → policy + value | [ ] | 甲，形状和乙对齐 |
| M3-2 | `RandomInfer`（均匀策略，value = 0） | [ ] | 甲 |
| M3-3 | 搜索树：选择（PUCT）、扩展、回传 | [ ] | 甲 |
| M3-4 | 用 MCTS 下一盘 | [ ] | 甲 |
| M3-5 | MCTS vs 随机，统计胜率 | [ ] | 甲跑，两人一起看 |

M3 验收：MCTS 胜率明显高于纯随机。  
本座不做：PyTorch、ONNX。模拟次数和 `c_puct` 可先写死。

---

## M4 Python 训练 `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M4-1 | `train/` 用 uv 固定 3.12，并装 torch / numpy / onnx | [ ] | 乙 |
| M4-2 | 特征编码（与 Java 同一套：己方/对方/轮次） | [ ] | 乙 |
| M4-3 | 读 JSON 棋谱的 Dataset | [ ] | 乙 |
| M4-4 | 小 CNN：policy + value 双头 | [ ] | 乙 |
| M4-5 | `train.py`：loss 能下降 | [ ] | 乙 |
| M4-6 | `export_onnx.py` → `models/dev.onnx` | [ ] | 乙 |

M4 验收：用 M2/M3 的棋谱训练，能导出 onnx。  
本座不做：Java 加载模型。

---

## M5 引擎用上模型 `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M5-1 | Java `OnnxInfer` 加载 `dev.onnx` | [ ] | 甲 |
| M5-2 | 同一棋面：Java / Python 输入张量一致（单测） | [ ] | 甲乙一起 |
| M5-3 | 带模型的 MCTS 能下一盘 | [ ] | 甲 |
| M5-4 | 和无网络 MCTS 下一组对比 | [ ] | 两人一起看棋 |

M5 验收：能加载模型开局；观感上少乱填、更会抢大场。  
本座不做：自动循环训练、GUI。

---

## M6 闭环（第一版完成） `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M6-1 | 批量自对弈，可指定盘数 | [ ] | 甲 |
| M6-2 | 新棋谱送进训练，产出新 onnx | [ ] | 乙 |
| M6-3 | 引擎换模型再打，不靠手工猜路径 | [ ] | 甲 |
| M6-4 | 一条脚本串起：自对弈 → 训练 → 再下 | [ ] | 甲乙各写自己那截，最后拼 |
| M6-5 | 按脚本跑通一整轮 | [ ] | 两人一起 |

M6 验收：闭环能重复转。第一版到此结束。

---

## 第一版之后（现在不排期）

| ID | 任务 |
| --- | --- |
| L-1 | 13 路，再 19 路 |
| L-2 | 更大网络、GPU 训练 |
| L-3 | 超级劫、更好的终局判断 |
| L-4 | GTP，接 Sabaki |
| L-5 | 简易 GUI / 网页看棋 |
| L-6 | 固定对手、Elo |

---

先定谁是甲、谁是乙，本周只收尾 M0。
