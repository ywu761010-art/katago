# katago 任务清单

总目标：9 路跑通「规则 → MCTS → 训练 → 再下棋」。第一版不含 19 路、GTP、GUI。

阶段顺序以 [路线图.md](路线图.md) 为准。接口以 [开发文档.md](开发文档.md) 为准。

图例：`[x]` 已完成 · `[~]` 进行中 · `[ ]` 未开始

---

## 两人怎么分

| 角色 | 负责 | 只改这些目录 |
| --- | --- | --- |
| **甲 · 引擎（你）** | Java 规则、对局、MCTS、加载模型 | `engine/` |
| **乙 · 训练** | Python 特征、网络、训练、导出 onnx | `train/` |

甲只在 `jia/*` 分支改 `engine/`，乙只在 `yi/*` 分支改 `train/`。命名规则见开发文档第 8.1 节。  
公共约定（JSON 字段、特征通道）先改开发文档，甲乙再一起改代码，不要各改各的。

乙不必等 MCTS。开发文档里的 JSON / 特征已经够乙用假棋谱开工。甲是关键路径：规则没对，后面全是空转。

---

## 大概多久

总工期 **约 2–3 周**，共约 **8–10 人天**（甲约 6，乙约 4）。按周分配、不拆到天；某周卡壳自动占下星期，别单周硬排。M1→M2→M3 必须串行，这段压不掉；调试类任务不要两边硬并行。AI 协作加速写码 / 查错 / 格式往返；**对齐、跑盘、调参、环境排错**这类要等真实结果的活压缩有限。

> 标记：`🐢` 难点 / 最可能拖慢，先啃、留弹性 · `⚠️` 环境或外部风险（网络、库、CPU 慢）。

### 按周分配

| 周 | 甲 · Java（`engine/`） | 乙 · Python（`train/`） | 对齐点 / 验收 |
| --- | --- | --- | --- |
| 1 | M0 收尾；M1 棋盘规则：气、提子、🐢劫、🐢数子、单测 | 特征编码 + 小 CNN + `train.py` 降 loss ⚠️ | 随机对下不崩溃；测试棋面 `[1,3,9,9]` |
| 2 | M2 随机对局出 JSON ⚠️；M3 🐢MCTS 修到明显赢随机 | `export_onnx.py` ⚠️ → `models/dev.onnx` | 棋谱字段和文档一致；Infer 形状锁死 |
| 3 | M5 ⚠️加载 onnx 下棋；M6 批量自对弈、换模型 | M5-2 🐢编码对齐单测；M6-2 吃新谱再训练 | 同一局面两边输入一致；脚本转完一整轮 |

---

## 第一周（已基本收尾，记要与备忘）

- 乙已达成：`train` 环境（uv 固定 3.12）、`encode` 输出 `[1,3,9,9]`、`example.json`（9 路 / 贴 7.5 / 3 手）、`tests` 通过；对应 M0-8、M4-1、M4-2 开头，已 commit / push。
- 待核对（甲侧 / 周末验收）：`engine` 能 `mvn compile`，命令行打印 9 路棋盘并落下合法一手。
- 共同已做：定甲/乙角色、补 `.gitignore`。未做：GitHub 推送认证（M0-6）。

### 环境备忘（乙，必读的坑）

- 本机只有 Python 3.14，torch 没 3.14 轮子 → 一律在 `train/` 下 `uv run ...`（uv 固定 3.12，`train/.python-version` 已配好）；`.venv/` 不提交。
- 写测试先 `uv add --dev pytest`，再 `uv run pytest tests`。
- src layout：模块在 `train/src/katago_train/`，测试在 `train/tests/`，导入名仍是 `katago_train.*`。
- 推 GitHub 偶发重置 / 超时 → 过会儿重试。

### 数据格式备忘（乙）

- 字段以 `开发文档.md` §3.3 为准：`size` / `komi` / `result` / `moves[]`（每手 `color` / `pos` / `policy` / `value`）。
- `pos = y*size + x`，pass = `size*size`；policy 长度 `size*size + 1`（9 路 = 82）。
- `data/selfplay/` 只有 `example.json` 和 `.gitkeep` 进 Git，其余自对弈棋谱被忽略。

---

## M0 立住项目 `[~]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M0-1 | 定方案 B：Java 规则/MCTS/引擎 + Python 训练/模型 | [x] | 共同 |
| M0-2 | 写开发文档（职责、接口、目录、约定） | [x] | 共同 |
| M0-3 | 写路线图（里程碑和验收） | [x] | 共同 |
| M0-4 | 写本任务清单（含两人排期） | [x] | 共同 |
| M0-5 | 本地仓库对齐 GitHub ⚠️（国内网络） | [~] | 有远程权限的人登录后 push |
| M0-6 | 配 GitHub 推送认证（Edge / PAT / SSH）⚠️ | [ ] | 有远程权限的人 |
| M0-7 | 建 `engine/`、`models/`、`data/selfplay/` | [x] | 甲 |
| M0-8 | 建 `train/`，补 `.gitignore` | [~] | gitignore 已补；`train/` 等乙 |
| M0-9 | 建 `engine/pom.xml`（JDK 17，先能编译空项目） | [x] | 甲 |

M0 收口：目录在、文档齐、能 `git push`。然后甲写棋盘，乙搭训练环境。

---

## M1 棋盘会走规则 `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M1-1 | 棋盘数据结构，`size` 可配，先 9 路 | [ ] | 甲 |
| M1-2 | 落子、气、提子 | [ ] | 甲 |
| M1-3 | 禁自杀、🐢简单劫、停着（pass） | [ ] | 甲 |
| M1-4 | 🐢终局数子 + 贴 7.5 目 | [ ] | 甲 |
| M1-5 | 合法点列表（给引擎和 MCTS 用） | [ ] | 甲 |
| M1-6 | 单测：边角提子、劫、自杀点 | [ ] | 甲 |
| M1-7 | 命令行人人对战或随机对下，能走完不崩溃 | [ ] | 甲写，两人各下一盘 |

M1 验收：非法棋下不出去；双方连 pass 能报谁赢。  
本座不做：AI、训练、19 路、超级劫。

---

## M2 能打完一盘并记下 `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M2-1 | 对局循环：轮流、pass、结束、认输 | [ ] | 甲 |
| M2-2 | 随机机器人（合法点里乱选） | [ ] | 甲 |
| M2-3 | 随机 vs 随机打完整盘 | [ ] | 甲 |
| M2-4 | 按开发文档把 JSON 写到 `data/selfplay/` ⚠️（字段与 3.3 节逐字一致） | [ ] | 甲写，乙确认能读 |

M2 验收：能看到棋谱文件，人能读懂。  
本座不做：MCTS、神经网络。

---

## M3 无网络 MCTS `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M3-1 | `Infer` 接口：局面 → policy + value | [ ] | 甲，形状和乙对齐 |
| M3-2 | `RandomInfer`（均匀策略，value = 0） | [ ] | 甲 |
| M3-3 | 🐢搜索树：选择（PUCT）、扩展、回传 | [ ] | 甲 |
| M3-4 | 用 MCTS 下一盘 | [ ] | 甲 |
| M3-5 | MCTS vs 随机，统计胜率 ⚠️（CPU 跑盘慢，盘数少结论不稳） | [ ] | 甲跑，两人一起看 |

M3 验收：MCTS 胜率明显高于纯随机。  
本座不做：PyTorch、ONNX。模拟次数和 `c_puct` 可先写死。

---

## M4 Python 训练 `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M4-1 | `train/` 用 uv 固定 3.12，并装 torch / numpy / onnx | [ ] | 乙 |
| M4-2 | 特征编码（与 Java 同一套：己方/对方/轮次） | [ ] | 乙 |
| M4-3 | 读 JSON 棋谱的 Dataset | [ ] | 乙 |
| M4-4 | 小 CNN：policy + value 双头 | [ ] | 乙 |
| M4-5 | `train.py`：loss 能下降 ⚠️（谱少/假谱可能不降，先拿 M2/M3 真谱） | [ ] | 乙 |
| M4-6 | `export_onnx.py` ⚠️ → `models/dev.onnx`（形状与 Java Infer 契约锁死） | [ ] | 乙 |

M4 验收：用 M2/M3 的棋谱训练，能导出 onnx。  
本座不做：Java 加载模型。

---

## M5 引擎用上模型 `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M5-1 | Java `OnnxInfer` 加载 `dev.onnx` ⚠️（onnxruntime Java jar/dll 依赖坑） | [ ] | 甲 |
| M5-2 | 🐢同一棋面：Java / Python 输入张量一致（单测） | [ ] | 甲乙一起 |
| M5-3 | 带模型的 MCTS 能下一盘 | [ ] | 甲 |
| M5-4 | 和无网络 MCTS 下一组对比 | [ ] | 两人一起看棋 |

M5 验收：能加载模型开局；观感上少乱填、更会抢大场。  
本座不做：自动循环训练、GUI。

---

## M6 闭环（第一版完成） `[ ]`

| ID | 任务 | 状态 | 谁 |
| --- | --- | --- | --- |
| M6-1 | 批量自对弈，可指定盘数 | [ ] | 甲 |
| M6-2 | 新棋谱送进训练，产出新 onnx | [ ] | 乙 |
| M6-3 | 引擎换模型再打，不靠手工猜路径 | [ ] | 甲 |
| M6-4 | 一条脚本串起：自对弈 → 训练 → 再下 ⚠️（两端拼装调试） | [ ] | 甲乙各写自己那截，最后拼 |
| M6-5 | 按脚本跑通一整轮 | [ ] | 两人一起 |

M6 验收：闭环能重复转。第一版到此结束。

---

## 第一版之后（现在不排期）

| ID | 任务 |
| --- | --- |
| L-1 | 13 路，再 19 路 |
| L-2 | 更大网络、GPU 训练 |
| L-3 | 超级劫、更好的终局判断 |
| L-4 | GTP，接 Sabaki |
| L-5 | 简易 GUI / 网页看棋 |
| L-6 | 固定对手、Elo |

---

里程碑进度看各表状态；排期按上面「按周分配」。
